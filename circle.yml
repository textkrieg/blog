general:
  branches:
    ignore:
      - gh-pages

machine:
  nodejs: 4.2.6

dependencies:
  pre:
    - npm install
    - cat npm-debug.log || true

test:
  override:
    - "true"

deployment:
  production:
    branch: master
    commands:
      - git config --global user.email "kontakt@textkrieg.de"
      - git config --global user.name "Ada Blitz"
      - GIT_SSH_COMMAND="ssh -i $HOME/.ssh/id_6dd138076ddd11dfbd2c8fb70bd6fe14" npm run deploy
